@page "/order/view-order-product"

<PageTitle>ViewOrderProduct</PageTitle>

<div class="outer-container">

    <h4>Order Produkt Oversigt</h4>

    <!-- Order Product Table -->
    <div class="table-container">

        <table class="table table-striped table-hover">
            <thead class="table-primary">
                <tr>
                    <th>Id</th>
                    <th>OrderId</th>
                    <th>ProductId</th>
                    <th>Quantity</th>
                    <th>UnitPrice</th>
                    <th>Fjern</th>
                </tr>
            </thead>

            @* <tbody>
                @foreach (var item in OrderDetails)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.OrderId</td>
                        <td>@item.ProductId</td>
                        <td>@item.Quantity</td>
                        <td>@item.UnitPrice</td>

                        <td>
                            <button class="btn btn-danger btn-sm">
                                Fjern
                            </button>
                        </td>
                    </tr>
                }
            </tbody> *@

        </table>

    </div>

</div>